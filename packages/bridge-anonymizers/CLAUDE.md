# bridge-anonymizers

Cairo anonymizer contracts (pool ↔ CCTP): `outbound_anonymizer` (pool → CCTP) and
`inbound_anonymizer` (CCTP → pool).

- Root rules & index: ../../CLAUDE.md

Source of truth for pool semantics is the `starknet-privacy` Cairo — mirror it, don't re-spec.
