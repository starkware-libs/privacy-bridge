# contracts-cairo

Cairo Anonymizer contract (Scarb workspace member). Placeholder — see
`../../docs/privacy-pool-integration.md`.

Build: `scarb build` · Test: `snforge test` (needs Starknet Foundry; see `../../.tool-versions`).
