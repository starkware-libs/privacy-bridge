# contracts-cairo

The Anonymizer helper contract (pool withdraw recipient ↔ CCTP).

- Root rules & index: ../../CLAUDE.md
- Deep dive: ../../docs/privacy-pool-integration.md

Source of truth for pool semantics is the `starknet-privacy` Cairo — mirror it, don't re-spec.
