# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.turbo/
*.tsbuildinfo

# env (only the committed template env.example is tracked)
.env
.env.*
!env.example
!.env.example

# cairo / scarb
target/
.snfoundry_cache/

# session-local scratch artifacts (screenshots, scratch output)
.artifacts/

# misc
.DS_Store
coverage/
*.log

# Scratch workflow harness scripts (session-local; not part of the repo)
.workflow-*.js

# These are checked-in team artifacts for this repo, overriding any global
# gitignore that excludes them (project instructions + Cursor Bugbot rules).
!CLAUDE.md
!.cursor/
!.cursor/BUGBOT.md
# Checked-in Claude Code skills (shared team review conventions), overriding a
# global gitignore that excludes `.claude/`.
!.claude/
!.claude/skills/
